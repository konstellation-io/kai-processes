metadata:
  product_id: GRPC Trigger product
  workflow_name: GRPC Trigger workflow
  process_name: GRPC Trigger process
  version_tag: GRPC Trigger version v1.0.0
  base_path: "/Some/Path"

nats:
  url: "nats://localhost:4222"
  stream: trigger
  output: grpc-trigger-output

minio:
  endpoint: "localhost:9000"
  access_key_id: minioadmin
  access_key_secret: minioadmin
  use_ssl: false
  bucket: test-bucket

centralized_configuration:
  global:
    bucket: global
  product:
    bucket: product
  workflow:
    bucket: workflow
  process:
    bucket: process